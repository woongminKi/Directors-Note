# 2026-06-04 영상분석 로직 진단 및 LLM-judge 개선 플랜

## 배경
어제(06-03) 다른 컴퓨터에서 reference 영상 6개를 3파트 구조로 재임베딩(seed)함.
영상은 학생 실기 평가본으로, 연기·몸짓·발성을 보려고 **멀리서 촬영** → 얼굴이 작게 잡힘.
"어떤 기준·로직으로 분석하는지" 파악 + 현재 로직 검증 + 개선 플랜 수립이 목표.

## DB 임베딩 현황 (dev)
- reference_video 임베딩 18행 = 6영상 × 3파트(part1/2/3), 전부 1408d·정규화(L2=1.0)·NULL 0.
- 임베딩 시각 06-03 07:52~07:57 UTC. 6개 모두 `scene_type=modern_monologue`, tier A×2/B×2/C×2.
- evaluation 임베딩은 05-29 1행(part=NULL, v0 레거시)뿐 → **어제 학생 평가 파이프라인은 안 돌았음**.

## 분석 로직 (현재 v1, 코드 기준)
파일: `src/lib/evaluation/{vertex.ts,grade-derivation.ts,gpt-4o-mini-letter.ts}`, `migrations/0013_*.sql`
1. 학생 영상 3파트 분할 임베딩(Vertex `multimodalembedding@001`, 1408d).
2. 파트별 cosine 매칭(RPC `search_reference_matches_by_part`, 같은 part끼리 top-5).
3. 점수 = `TIER_BASE[매칭ref등급] + (cos-0.5)*3` clamp 0~10. TIER_BASE={A:8,B:6.5,C:5,D:3.5}.
4. part1→표정(expression), part2→발성(vocal), part3→입시완성도(examReadiness).
5. 등급 = 3축 평균: A≥7.25 / B≥5.75 / C≥4.25 / else D.
6. gpt-4o-mini는 **채점 안 함** — 점수·등급을 받아 부모 letter만 작성. `llm_as_judge` 경로는 함수만 있고 미연결(TODO D12).

→ **핵심: "연기를 평가"하는 게 아니라 "사전 등급이 매겨진 reference와 닮음(cosine)"만 봄.** 축 이름은 시간대 라벨일 뿐.

## 실측 검증 결과 (결정적)
- WITHIN-PART cosine(다른 영상 같은 part) ~0.88, CROSS-PART(같은 영상 다른 part) ~0.91.
- 같은 tier 쌍 ≈ 다른 tier 쌍 → **cosine에 등급 변별력 없음**.
- 6개를 각각 "학생"으로 넣고 production 공식 그대로 적용 → **tier match 1/6 = D13 환산 1.7/10** (게이트 ≥7/10 압도적 미달).
- 등급이 전부 A로 붕괴. 실제 C급이 A급 ref에 cos 0.91~0.95로 매칭 → A.
- 원인: 원거리 촬영으로 표정·발성 미세신호가 1408d 전체프레임 임베딩에서 장면·전신·배경에 묻힘 + cosine은 "품질"이 아닌 "장면 유사도"를 잼.
- 캐비엇: n=6, reference끼리 비교라 통계 단정 불가. 단 "자기 reference의 A/B/C조차 못 가른다"는 구조적 한계 입증.

## 개선 플랜 (확정 결정 포함)
계측기 교체가 답. **LLM-as-judge(멀티모달)를 primary 채점기로** — D13이 명령하는 fallback 그 자체.
아키텍처가 이미 준비됨: `evaluatorUsed: 'llm_as_judge'`가 타입·DB CHECK에 존재, 라우트는 nodejs+maxDuration=300. **DB 마이그레이션/UI 계약 변경 불필요.**

### 사장님 확정 결정 (2026-06-04)
- **모델 = Gemini-on-Vertex.** 같은 GCP 프로젝트/리전(임베딩 동의 범위), GCS 영상 그대로 입력, 오디오 포함(발성 커버), PIPA 변경 최소.
- **비용 상한 = 품질 우선 ~$0.10/건.** 풀영상·파트별 호출·borderline self-consistency 허용.
- **근접 재촬영 = 가능.** 신규 업로드부터 근접 촬영 가이드 병행.

### 단계
- **Phase 0**: D13 calibration 하니스 스크립트(leave-one-out tier-match). 모든 후보 비교 게이트. 오프라인·무비용. `scripts/` 위치.
- **Phase 1**: `llm-judge.ts`(VideoAnalysisService 구현) + `prompts/judge-rubric-v1.ts`(코치 전용 한국어 루브릭, 구조화 JSON) + GCS staging 공통 헬퍼 추출. `factory.ts`에 evaluator 선택 env. 기존 `deriveGradeFromScores` 등급 경계 재사용. Phase0 하니스로 ≥7/10 검증.
- **Phase 2**: 근접 촬영 가이드(저비용 병행).
- **Phase 3 (v2)**: 축별 하이브리드(발성 전용 오디오 등) + 비용 최적화. cosine은 삭제 X — shadow 텔레메트리 + `FEATURE_AI_VIDEO_ANALYSIS=false` Approach-A fallback 유지.

### P2 준수
등급·루브릭은 코치 전용(`ai_analyses`)에만, 부모 letter 경로는 손대지 않음 → AI 등급 부모 비노출 유지.

### 재현성(정합성) 요구사항 — 사용자 추가 요청 (2026-06-04)
**요구: 같은 영상은 언제·어느 머신에서 돌려도 같은 임베딩/점수가 나와야 한다.** (provenance ③는 사용자 확인 완료 — 동일 원본 업로드)
관찰된 "같은 영상 점수 차이"의 유력 원인: v0(0-120s 단일창) → v2(3파트) **방법론 변경** 또는 Vertex 비결정성. 둘 다 대응 필요.
- **임베딩 계층**: 모델 버전 핀(`multimodalembedding@001` 고정), 리전 고정, part window 고정. **Vertex video 임베딩 결정성 1회 실측**(같은 파일 2회 임베딩 → cos≈1.0 확인). 임베딩은 이미 `cacheEvaluationEmbedding`으로 캐시됨.
- **judge 계층(신규 primary, 재현성 핵심)**: LLM은 기본 stochastic → `temperature=0`/결정적 디코딩, 프롬프트·루브릭 **버전 고정**, structured-output 스키마 강제, **(영상해시, 모델버전, 루브릭버전)별 점수 캐시**(재호출 시 동일 결과), borderline만 self-consistency.
- **provenance 스탬프**: `rawResponseJson`에 모델버전·루브릭버전·임베딩방법 기록 → 재실행 비교 가능, 점수 drift 추적.
- **Phase 0 하니스에 재현성 지표 추가**: 같은 입력 N회 → 점수 분산 ≈ 0 검증(게이트). tier-match와 별개 축.

**결정성/재현성 실측 결과 (2026-06-04, downloads/상1.mp4 재임베딩):**
- **Vertex 결정성: 완전 결정적.** 같은 파일 2회 호출(A vs B) → 3파트 모두 cos = **1.000000**.
- **재현성: 완벽.** 지금 임베딩 vs DB(어제 다른 컴퓨터) → 상1이 DB #1A와 3파트 모두 cos = **1.0000** 정확 일치. (부수 확인: #1A=상1, 시드가 파일명 순서대로 들어감)
- **결론: 임베딩 계층 재현성 문제 없음(해결).** 관찰됐던 "같은 영상 점수 차이"는 비결정성이 아니라 **방법론 변경(v0 단일창 → v2 3파트)** 때문. 모델버전·리전·part window 핀 고정 + 기존 임베딩 캐시면 충분, 추가 완화 불필요.
- **→ 재현성 리스크는 이제 전적으로 judge 계층(LLM stochastic)에 집중.** temperature=0 + 프롬프트/루브릭 버전핀 + 점수 캐시 + borderline self-consistency 가 재현성 작업의 실체. Phase0 재현성 게이트는 judge 점수 분산 검증에 집중.
- 실측 스크립트: `/tmp/determinism.ts` (세션용, Vertex 6콜 ≈ $0.006 소요).

## 산출물
- `scripts/check-embeddings.ts` — 읽기 전용 임베딩 점검(추가, 미커밋).
- 임시 분석: `/tmp/{pairwise,tier,analyses,simulate}.ts` (세션용).

## Phase 0 구현 완료 (2026-06-04, 미커밋)
D13 calibration 하니스 구축 — production `grade-derivation.ts`를 직접 import해 leave-one-out tier-match 측정.
- `scripts/calibration-harness.ts` — self-excluding pgvector NN per part → 실제 `buildAnalysisFromPartMatches` 채점. evaluator-agnostic seam(`CalibrationCase`/`Evaluator`) + `cosineLeaveOneOutEvaluator` + `// TODO(Phase1): judgeEvaluator`. 플래그 `--strict`(미달 시 exit 1), `--repro`(재현성 분산).
- `scripts/calibration-metrics.ts` — DB-free 순수 지표(tier-match율, D13 점수/게이트, confusion matrix, repro spread).
- `tests/unit/evaluation/calibration-metrics.test.ts` — 12 케이스.
- `package.json` — `calibrate` 스크립트.
- **검증**: `bun run calibrate` → tier-match **1/6 = 1.7/10 FAIL** 재현. test 12/12, typecheck clean, biome clean, `--strict` exit 1, `--repro` 분산 0(cosine 결정적).
- **추가 발견**: confusion matrix상 cosine이 **거의 모두 A로 붕괴**(6중 5예측 A). 원인 = `scorePartFromTopMatch`의 최근접 이웃 tier × cosine 보너스(+1.35)가 정규화 없이 점수를 A대(≥7.25)로 밀어올림. 무작위 오류가 아닌 **체계적 상향 편향**.

## Phase 1 준비 — 루브릭 v1 확정 (2026-06-04)
downloads 6개 영상 18프레임 직접 관찰 → 환경 확인(고정 광각 풀샷, 단독 연기자, 얼굴 작음, p1 자유연기/p2 무용/p3 면접, 정지프레임으론 상중하 구분 불가).
- **루브릭 v1 확정**: `2026-06-04 judge 채점 루브릭 v0.1 초안.md` (4축 발성·표정·몸짓·입시완성도, 각 0–10, A8-10/B6-7.5/C4-5.5/D0-3.5, 4축평균→기존경계). 기본값: 노래없음·몸짓 rawResponseJson 보관·4축평균.
- 핵심 설계: "원거리·작은 얼굴 → 얼굴로 감점 금지, 표정은 신체+음성으로 추론" / 발성=오디오 / temperature=0+버전핀+캐시(재현성).
- **원장 calibration 문서 발송용 작성**: `2026-06-04 원장님 채점 가이드.md` (쉬운 채점 가이드 + 6영상 채점표). 코치 채점 = ground-truth → 밴드 보정 + judge 검증 기준.

## 다음 액션
1. (대기) 원장 친구 6영상 채점 회수 → ground-truth 확보 → 루브릭 밴드 보정.
2. Phase 1 구현 — Gemini-on-Vertex LLM-judge를 `judge-rubric-v1.ts` + `llm-judge.ts`로 `Evaluator` seam에 구현 → calibration 하니스로 ≥7/10 + `--repro` 분산 0 검증. Vertex 호출(비용/권한) 필요.
