# 2026-06-02 학생 상세 인라인 부모동의 안내

## 작업 내용
학생 상세 페이지에서 부모 동의가 기록되지 않은 동안만 "다음 할 일 — 부모 동의서" 인라인 안내 카드를 띄우고, owner/admin이 확인 다이얼로그를 거쳐 동의를 기록하면 카드가 사라지고 "평가 시작"이 활성화되도록 구현. (브레인스토밍 → spec → plan → subagent-driven 구현 + 2단계 리뷰)

- spec: `docs/superpowers/specs/2026-06-02-consent-guidance-inline-design.md`
- plan: `docs/superpowers/plans/2026-06-02-consent-guidance-inline.md`
- 브랜치: `feat/consent-guidance-inline`

## 변경
- **`src/lib/students/actions.ts`** — `recordParentConsent(id)` 신규 server action. owner/admin 가드(`requireRole`), academy 격리, soft-delete 가드, **멱등**(이미 기록 시 no-op), `parentConsentOnFileAt=now` + `parentConsentVersion=CURRENT` stamp, `/students`·`/students/[id]` revalidate. 스키마 변경 없음.
- **`src/app/(coach)/students/[id]/consent-guidance-card.tsx`** — 신규 client 컴포넌트. `<Alert>`(amber) + `AlertTitle`/`AlertDescription`, "동의서 전문 보기"(→`/parent-consent` 새 탭), owner/admin에게만 "동의 받음으로 표시" 버튼 → 확인 다이얼로그(`DialogDescription` a11y, 취소/확인 모두 pending 시 disabled) → 성공 시 `router.refresh()`로 카드 자동 소멸.
- **`src/app/(coach)/students/[id]/page.tsx`** — `!student.parentConsentOnFileAt`일 때 평가시작 버튼 위에 카드 렌더. `canRecordConsent={canManage}`.
- **테스트** — `tests/integration/students/actions.test.ts`에 `recordParentConsent` 5건(stamp/멱등 no-op/not-found/owner·admin 게이트/soft-delete). 컴포넌트는 RTL 하니스 없어 typecheck+수동 검증.
- **부수 수정** — `tests/unit/dashboard/empty-state*`의 stale 단언 `/admin/users/new`→`/users/new` (라우트는 진작 변경됐는데 테스트만 낡아 main CI 빨강이던 것). 실제 코드/링크는 정상이었음.

## 검증
- lint ✅ / typecheck ✅ / test:ci **114 pass · 1 skip · 0 fail**
- spec 준수 리뷰 + 코드 품질 리뷰(태스크별) + 전체 최종 리뷰 = Ready to merge

## 결정 사항
- 범위: 동의 단계만(평가/업로드 단계 안내 X), 학생 상세에서만, 인라인 즉시 확인 + 확인 다이얼로그.
- 동의는 한 번 기록되면 카드 미렌더(= 안 보임), 미기록 시 계속 노출.
- AI 등급/분석은 코치 전용(P2) — 본 기능과 무관, 변경 없음.
