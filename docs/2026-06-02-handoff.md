# Handoff — 2026-06-02 (파비콘 + 친구 owner 온보딩 + 평가기준 6개 등록 + 인라인 동의 안내)

이 문서 하나로 이어서 작업할 수 있게 정리한 세션 핸드오프. 직전 상태는 `docs/2026-05-29-handoff.md`, backlog는 `TODOS.md` 참조.

---

## 0. 한 줄 요약

파비콘을 적용·배포하고, **원장 친구(`hb1834@hanmail.net`)를 카타르시스 owner로 온보딩**(재로그인 대기)했으며, 친구가 보낸 **평가 기준 영상 6개를 reference로 등록(2A/2B/2C, Vertex 1408d 임베딩)**해 학생 영상 분석이 가능한 상태를 만들고, **학생 상세 "인라인 부모동의 안내" 기능**을 브레인스토밍→spec→plan→구현→리뷰→배포까지 완료해 prod에 올렸다.

---

## 1. 이번 세션(6/2)에 한 일

### 1.1 파비콘
- favicon_io 세트(favicon.ico + PNG 5종 + site.webmanifest) 적용, `src/app/layout.tsx` metadata에 icons/manifest 와이어링. → commit `6f89ef6`.
- 재생성된 세트로 6종 전체 교체. → commit `796b1c2`.
- `.gitignore`에 `downloads/`(reference 영상 폴더, PIPA) + 이후 `.superpowers/`(브레인스토밍 산출물) 무시 추가.
- 모두 `origin/main` 푸시 → Vercel 자동배포 확인.

### 1.2 자동배포 구성 확인
- **Vercel Git 연동 = main 푸시 시 프로덕션 자동배포** 확정 (`directors-note-git-main-...` alias로 검증). GitHub Actions(`.github/workflows/ci.yml`)는 lint/typecheck/test만, 배포는 Vercel 담당(CI와 독립).

### 1.3 친구(원장) owner 온보딩 — `hb1834@hanmail.net`
- 친구가 카카오 로그인 1회 → not-invited 도달(정상, `auth.users` 생성).
- **`public.users`에 owner 등록 완료** (사용자가 Supabase SQL Editor에서 직접 실행, 6/2 10:55 KST). `auth.users.id`(`03727ace…`)와 일치, 학원=카타르시스(`554c68ef…`). owner 2명(친구 + 본인 `rldndals`).
- **남은 액션: 친구 재로그인** → `/dashboard` owner 진입 (마지막 로그인이 등록 전이라 세션 갱신 필요). 재로그인 시 `display_name`이 카카오 닉네임 **"Can Do"**로 backfill됨.

### 1.4 평가 기준(reference) 영상 6개 등록
- 기존 테스트 시드 `3b5ee32a`(tier A smoke-test) 삭제(사용자 SQL Editor, embeddings CASCADE). Storage 파일은 orphan으로 잔존(무해).
- 친구가 카톡으로 보낸 6개를 `downloads/`(상1·상2·중1·중2·하1·하2)로 정리 → `scripts/seed-reference-video.ts`로 등록.
- **tier 매핑: 상→A, 중→B, 하→C** (점수 base 8.0/6.5/5.0, D 미사용). 전부 `modern_monologue`, technique_tag "원장 기준영상".
- 결과: **2A/2B/2C, 6개 모두 Vertex 1408d 임베딩 정상** (DB 검증). scene_type은 코사인 매칭에 영향 없음(라벨용).
- 기록: `work-log/2026-06-02 평가기준 reference 영상 등록.md`.

### 1.5 인라인 부모동의 안내 기능 (신규)
- 흐름: 브레인스토밍(시각 컴패니언) → spec → plan → subagent-driven 구현(태스크별 spec+품질 2단계 리뷰) → 최종 리뷰 → main 병합 → 푸시 → 배포.
- spec: `docs/superpowers/specs/2026-06-02-consent-guidance-inline-design.md`
- plan: `docs/superpowers/plans/2026-06-02-consent-guidance-inline.md`
- work-log: `work-log/2026-06-02 학생상세 인라인 부모동의 안내.md`
- 동작: 학생 상세에서 `parentConsentOnFileAt == null`이면 "다음 할 일 — 부모 동의서" 카드 → owner/admin이 확인 다이얼로그로 기록 → 카드 사라지고 "평가 시작" 활성. 기록되면 미렌더(안 보임), 미기록이면 계속 노출.
- 변경: `recordParentConsent` server action(멱등·owner/admin·academy 격리·soft-delete 가드) / `consent-guidance-card.tsx` / `students/[id]/page.tsx`. 스키마 변경 없음.
- 부수: stale 대시보드 테스트 2건(`/admin/users/new`→`/users/new`) 수정 — main CI 빨강이던 것 복구(실제 링크/코드는 정상이었음).
- 검증: lint ✅ / typecheck ✅ / test:ci **114 pass · 1 skip · 0 fail**. main `2f70ada`.

---

## 2. 현재 prod 상태 (변경분)

- 파비콘 라이브.
- **카타르시스 reference set = 6개 (2A/2B/2C)** — 학생 영상 분석의 코사인 매칭 기준 확보됨 (기존 1개 테스트 시드는 제거).
- **친구 owner 등록됨** (재로그인 대기).
- **인라인 동의 안내 기능 배포됨** (main `2f70ada`, Vercel 자동배포).
- DB/인프라/Kakao/Vertex 등 나머지는 `docs/2026-05-29-handoff.md §2`와 동일 (dev=prod 동일 Supabase `kyizppeuvalqjtnhyqgf`, 함수 리전 icn1, `FEATURE_AI_VIDEO_ANALYSIS=true`).

---

## 3. 앞으로 해야 할 작업

### 3.1 즉시 (이번 세션에서 열린 루프)
1. **친구 재로그인 확인** — owner로 `/dashboard` 진입되는지. 안 되면 카카오 가입 이메일이 `hb1834`와 다른 경우 등 별도 진단 필요.
2. **인라인 동의 안내 배포 후 수동 QA** (owner): 미동의 학생 상세 → 카드 보임 → "동의 받음으로 표시" → 다이얼로그 확인 → 카드 사라지고 평가시작 활성 / 이미 동의된 학생 카드 없음 / coach는 버튼 없이 문구·링크만.
3. **첫 실제 학생 영상 분석 1건** — reference와 **다른** 영상으로 (동일 영상은 cosine 100%→A self-match). 등급/3축/코사인 분포 확인.

### 3.2 reference 분석 파이프라인 (등록은 됐으나 분석 검증 필요)
- **Migration 0010 적용 여부 확인** — `search_reference_matches` RPC. seed 스크립트는 embeddings를 직접 INSERT하므로 등록엔 RPC 불필요했지만, **학생 영상 분석 시엔 이 RPC가 필수**. 미적용이면 분석 시 `function search_reference_matches does not exist` → D8 degrade. 첫 분석 전 Supabase SQL Editor에서 적용 확인. (TODOS "Vertex Approach-B follow-ups")
- **reference tier 다양화** — 현재 A/B/C 2개씩(6개). 의미있는 매칭엔 academy당 10-20개 권장(TODOS). D tier 사용 여부도 결정 필요(현재 미사용 → 학생이 D 못 받음).
- **D12 LLM-as-judge escalation 와이어링** — `shouldEscalateToJudge()` 헬퍼만 있고 caller 미연결. 첫 분석에서 cosine 분포 본 뒤. (후순위)
- **V1 axes 한계** — 단일 임베딩이라 vocal/expression/examReadiness 3축 동일 점수. axis별 reference 시드로 분리(v2). 친구가 축별 점수 요구 시.

### 3.3 prod 실사용 전 (친구 학원 정식 오픈 전)
- **PIPA 자문** — 생체정보(영상 AI) 처리 동의. 코드와 병렬 약속(CLAUDE.md PIPA note). 학생 등록 시 부모 동의서에 생체정보 처리 동의 포함.
- **Privacy policy 변호사 검토** — `src/app/privacy/page.tsx` v1 초안 + amber 배너 상태. 검토 후 배너 제거 + AI 영상분석 §1 생체정보 섹션 + parent consent v2 와이어링. (TODOS)
- **GCP 서비스 계정 키 iCloud sync 위험** — 키 파일 위치 점검(`.env.local`은 inlined JSON이라 파일만 이동하면 OK). (TODOS)
- **C2/C3 prod 결정** — 커스텀 도메인 vs vercel.app, Kakao app 단일/다중 전략. `docs/production-deploy-plan.md`.

### 3.4 하드 게이트 (CLAUDE.md)
- **Week-2 calibration kill-criterion** — 비학생 데이터에서 ≥7/10 tier match(D13). 실패 시 LLM-as-judge primary 전환. → reference 6개로 첫 분석 시작되면 이 측정 가능해짐.
- **Week-6 revenue gate** — 페이드 학원 0이면 Approach-A fallback ship.

### 3.5 잡다 / 선택
- localhost 카카오 로그인 쓰려면 Supabase Redirect URLs에 dev 포트 추가.
- cold start (Hobby 첫 요청 ~1초) — keep-warm cron 또는 Pro.
- orphan storage file `…/reference/3b5ee32a….mp4` 정리(무해, 선택).
- `display_name "Can Do"` 노출 검토 — 친구가 직접 letter 작성 시 부모 공유링크에 노출. 실명/직함 원하면 1줄 UPDATE.

---

## 4. 주의 / 함정

1. **dev = prod (같은 Supabase).** 로컬 DB 변경이 곧 prod. reference/학생 데이터 조심.
2. **main 푸시 = 프로덕션 자동배포.** 푸시 전 lint/typecheck/test 통과 확인.
3. **prod DB 직접 쓰기/삭제는 auto-mode 분류기가 차단할 수 있음** (이번 세션 경험). 안전한 경로: Supabase **SQL Editor**에서 사람이 실행, 또는 `.claude/settings.local.json`에 사용자가 직접 권한 추가(에이전트 자가 추가는 차단됨). 읽기(SELECT)/seed 스크립트는 통과됨.
4. **커밋/푸시는 사용자 명시 요청 시에만** (CLAUDE.md). subagent-driven 구현 중 태스크별 커밋은 이번에 명시 승인받음 — 다음에도 동일 워크플로면 사전 확인.
5. **reference self-match** — 시드한 영상과 동일 영상을 학생으로 올리면 cosine 100%·A.
6. **AI 등급은 코치 전용** (P2) — 부모 surface 노출 금지.

---

## 6. 다음에 TODOS.md에 반영할 항목 (이번 세션 미반영 — 다음에 갱신)

`TODOS.md`는 이번 세션에 손대지 않았다. 다음 작업 때 아래를 현실에 맞게 갱신할 것:

- **line 92 "친구 학원 reference 영상 촬영 + 시드"** → `[x]` 완료로 변경. 6개 등록(2A/2B/2C, `modern_monologue`, Vertex 1408d). 단 TODOS가 권장한 "academy당 10-20개"엔 못 미치므로 "추가 다양화 + D tier 여부"는 별도 열린 항목으로 남길 것.
- **line 91 "Vertex smoke test 실행"** → 사실상 검증됨. 실제 6개 시드가 OAuth→GCS→Vertex predict→DB 전 경로를 성공적으로 통과(= smoke test 대체). `[x]` 또는 "6/2 reference 시드로 검증됨" 주석.
- **친구 owner 온보딩** → `docs/2026-05-29-handoff.md §5` 런북대로 완료(로그인→SQL→재로그인 대기). TODOS엔 별도 라인 없으면 추가 불필요, 핸드오프로 충분.
- **line 90 "Migration 0010 적용"** → **여전히 미확인 상태로 유지**. reference 등록은 RPC 없이도 됐지만 학생 영상 *분석*엔 필수. 첫 분석 전 적용 확인 후 갱신.
- (참고) line 15 "Reference library admin UI" → `/reference` UI는 5/29에 이미 구현됨. after-v1 멀티academy 메타스키마 항목으로 범위 조정 가능.

---

## 5. 다른 컴퓨터 첫 메시지 템플릿

```
Director's Note 이어서 작업. docs/2026-06-02-handoff.md 읽고 현재 상태 파악해.
.env.local / .env.local.prod 복원돼 있고, 3000 비었으면 bun run dev.
다음: 친구 재로그인 확인 + 인라인 동의 안내 수동 QA + reference로 첫 학생 영상 분석 1건(다른 영상으로).
분석 전 migration 0010(search_reference_matches RPC) 적용 여부 확인.
```
